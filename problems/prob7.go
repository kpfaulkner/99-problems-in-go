package problems

import "fmt"




func prob7() {
  fmt.Printf("prob7\n")

  // wouldn't be a realistic scenario in go. Cant mix lists of ints, int, and lists of int + list/ints.
  // I could go down route of empty interfaces, but I'd never use that practically.
  // I'd make sure the data is in a sane order to begin with.

}

